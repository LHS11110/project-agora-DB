# Agora Database Infrastructure Root Compose
# 각 서비스(elasticsearch, redis, mssql)의 Compose 설정을 통합 관리합니다.
include:
  - path: ./elasticsearch/docker-compose.yml
  - path: ./redis/docker-compose.yml
  - path: ./mssql/docker-compose.yml
